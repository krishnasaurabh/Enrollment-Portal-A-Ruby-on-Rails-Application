<p id="notice"><%= notice %></p>

<h1><% if @course_name %> <%= @course_name %> <%end%> Enrollments</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @enrolled_students.each do |enrolled| %>
      <tr>
        <td><%= enrolled.student.user.name %></td>
        <td><%= enrolled.student.user.email %></td>
        <td><%= link_to 'Drop', enrolled, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
